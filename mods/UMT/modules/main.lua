local From = import('linq.lua').from
local Range = import('linq.lua').range
import('Importer.lua')

function init(isReplay)
    --import('tests.lua').main()
end

local Bitmap = require.lua.maui.bitmap().Bitmap